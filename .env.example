# Office to run with Docker: the name of a folder in ./offices
OFFICE=

# Run each agent in its own container (see docker-compose.override.example.yml)
SANDBOX=none

# API keys (not needed for local llama.cpp / vLLM models)
OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# GEMINI_API_KEY=
